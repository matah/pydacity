from distutils.core import setup
setup(
  name = 'pydacity',
  packages = ['pydacity'], # this must be the same as the name above
  version = '0.1.0',
  description = 'Wrapper around ffmpeg',
  author = 'Selma Ardelean',
  author_email = 'selma.ardelean@gmail.com',
  url = 'https://github.com/matah/pydacity', # use the URL to the github repo
  download_url = 'https://github.com/matah/pydacity/archive/0.1.0.tar.gz', # I'll explain this in a second
  keywords = ['ffmpeg', 'audio'], # arbitrary keywords
  classifiers = [],
)
